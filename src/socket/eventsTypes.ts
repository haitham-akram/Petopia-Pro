
// emits types for config file
export default {
  JOIN_ROOM: ["joinRoom"],
  SEND_MESSAGE: ["sendMessage"],
  SEND_PRIVATE_MESSAGE: ["sendPrivateMessage", "privateMessage"],
  LEAVE_ROOM: ["disconnect"],
};
